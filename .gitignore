# Secrets / local env — NEVER commit (this repo is public)
.env
.env.*
*.env

# Python
__pycache__/
*.pyc

# Editor / OS junk
*~
*.un~
.DS_Store
Thumbs.db

# LaTeX intermediates (the compiled PDF is committed; build files are not)
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

# Local scratch notes — not part of the skill deliverable.
# Remove this line if you want to publish it.
skill.txt

.env

